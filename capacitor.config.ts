import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Codigo-QR-V3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
